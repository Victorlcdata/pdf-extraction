# Copy this file to `.env` and fill in only the keys for the provider(s) you'll use.
# Then load them with: `set -a; source .env; set +a` before running the CLI.

# Anthropic Claude — https://console.anthropic.com
ANTHROPIC_API_KEY=

# Google Gemini — https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# OpenAI GPT — https://platform.openai.com/api-keys
OPENAI_API_KEY=
